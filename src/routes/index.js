const userRouter = require('./user.route');
const categoryRouter = require('./category.route');
const productRouter = require('./product.route');

module.exports = { userRouter, categoryRouter, productRouter };
